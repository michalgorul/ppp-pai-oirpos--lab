## Running server

### runserver
In the root folder just type in terminal:
```bash
.\venv\Scripts\flask.exe --app flask_lab/app run
```

## Linter
In the root folder just type in terminal:
```bash
mypy flask_lab
```

## Format
In the root folder just type in terminal:
```bash
black flask_lab
```
